import {
  "dowdiness/canopy/core" @core,
  "dowdiness/canopy/lang/lambda/proj" @lambda_proj,
  "dowdiness/canopy/lang/lambda/eval" @lambda_eval,
  "dowdiness/canopy/lang/lambda/companion" @lambda_companion,
  "dowdiness/canopy/protocol" @protocol,
  "dowdiness/diagnostic" @diagnostic,
  "dowdiness/lambda" @lambda,
  "dowdiness/lambda/ast" @ast,
  "dowdiness/loom" @loom,
  "dowdiness/loom/core" @loom_core,
  "examples/spikes/incr_next_cutoff_backdating/provider" @next,
  "moonbitlang/core/bench",
}

pkgtype(kind: "executable")
