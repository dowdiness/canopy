name = "dowdiness/canvas-spatial"

version = "0.1.0"

readme = "README.md"

repository = "https://github.com/dowdiness/canopy"

license = "Apache-2.0"

keywords = [ "canvas", "spatial", "geometry", "viewport", "moonbit" ]

description = "Finite 2D coordinates, viewport transforms, and pan/drag snapshots for MoonBit canvases."

preferred_target = "native"

supported_targets = "js+native"
